import {useState,type FormEvent} from 'react'
import {Link} from 'react-router-dom'
import {useAuth} from '../hooks/useAuth'
export default function ForgotPassword(){const{requestPasswordReset}=useAuth();const[email,setEmail]=useState('');const[msg,setMsg]=useState('');const[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');setMsg('');try{await requestPasswordReset(email);setMsg('If an account exists, reset instructions have been sent.')}catch(x){setError(x instanceof Error?x.message:'Unable to send reset email.')}}return <main className="min-h-screen bg-slate-950 text-slate-100 grid place-items-center p-6"><section className="w-full max-w-md rounded-3xl border border-slate-800 bg-slate-900 p-8"><h1 className="text-2xl font-bold">Reset your password</h1><form className="mt-7 space-y-5" onSubmit={submit}><input className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3" type="email" placeholder="Email" required value={email} onChange={e=>setEmail(e.target.value)}/>{msg&&<div className="text-emerald-300">{msg}</div>}{error&&<div className="text-red-300">{error}</div>}<button className="w-full rounded-xl bg-blue-600 px-4 py-3 font-semibold">Send Reset Email</button></form><div className="mt-6 text-center"><Link className="text-blue-400" to="/login">Return to sign in</Link></div></section></main>}
