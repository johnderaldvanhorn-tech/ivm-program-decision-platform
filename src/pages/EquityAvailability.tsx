import { useEffect,useMemo,useState } from 'react'
import { Card } from '../components/ui'
import { loadPhase2Machines,type Phase2Machine } from '../lib/phase2'
export default function EquityAvailability(){const [m,setM]=useState<Phase2Machine[]>([]);useEffect(()=>{loadPhase2Machines().then(setM)},[]);const agencies=useMemo(()=>Object.values(m.reduce((a:any,x)=>{const k=x.agency;a[k]??={agency:k,machines:0,dispensed:0,stockouts:0,capacity:0,current:0};a[k].machines++;a[k].dispensed+=x.units_dispensed;a[k].stockouts+=x.stockouts;a[k].capacity+=x.capacity;a[k].current+=x.current_quantity;return a},{})),[m]) as any[];return <div className="space-y-6"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-violet-600">Phase 2 — Research & Validation</p><h1 className="text-3xl font-bold">Equity & Availability</h1><p className="text-slate-500">Compare access, operational availability, and service burden across agencies and communities.</p></div><Card><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="p-3">Agency</th><th>Machines</th><th>Dispensed</th><th>Stockouts</th><th>Capacity</th><th>Observed fill</th><th>Availability proxy</th></tr></thead><tbody>{agencies.map(a=><tr className="border-t" key={a.agency}><td className="p-3 font-semibold">{a.agency}</td><td>{a.machines}</td><td>{a.dispensed}</td><td>{a.stockouts}</td><td>{a.capacity}</td><td>{a.capacity?(a.current/a.capacity*100).toFixed(1):'—'}%</td><td>{(100-a.stockouts/Math.max(1,a.dispensed)*100).toFixed(1)}%</td></tr>)}</tbody></table></div></Card></div>}
