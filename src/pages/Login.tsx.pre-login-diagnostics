import {useState,type FormEvent} from 'react'
import {Link,Navigate,useLocation,useNavigate} from 'react-router-dom'
import {useAuth} from '../hooks/useAuth'
export default function Login(){const{session,login,loading}=useAuth();const location=useLocation();const navigate=useNavigate();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[busy,setBusy]=useState(false);const[error,setError]=useState('');const from=(location.state as {from?:string}|null)?.from||'/';if(!loading&&session)return <Navigate to={from} replace/>;async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await login(email,password);navigate(from,{replace:true})}catch(x){setError(x instanceof Error?x.message:'Sign in failed.')}finally{setBusy(false)}}return <main className="min-h-screen bg-slate-950 text-slate-100 grid place-items-center p-6"><section className="w-full max-w-md rounded-3xl border border-slate-800 bg-slate-900 p-8 shadow-2xl"><div className="mb-5 grid h-14 w-14 place-items-center rounded-2xl bg-blue-600 text-xl font-black">IVM</div><h1 className="text-2xl font-bold">Sign in to IVM Program</h1><p className="mt-2 text-sm text-slate-400">Use your authorized account to access the decision platform.</p><form className="mt-7 space-y-5" onSubmit={submit}><input className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3" type="email" placeholder="Email" required value={email} onChange={e=>setEmail(e.target.value)}/><input className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3" type="password" placeholder="Password" required value={password} onChange={e=>setPassword(e.target.value)}/>{error&&<div className="rounded-xl border border-red-900 bg-red-950/50 p-3 text-sm text-red-200">{error}</div>}<button className="w-full rounded-xl bg-blue-600 px-4 py-3 font-semibold" disabled={busy}>{busy?'Signing in…':'Sign In'}</button></form><div className="mt-6 text-center text-sm"><Link className="text-blue-400" to="/forgot-password">Forgot password?</Link></div><p className="mt-8 text-center text-xs text-slate-500">IVM Program v1.0.0</p></section></main>}
