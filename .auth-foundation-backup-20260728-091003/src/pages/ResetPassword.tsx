import {useState,type FormEvent} from 'react'
import {useNavigate} from 'react-router-dom'
import {useAuth} from '../hooks/useAuth'
export default function ResetPassword(){const{updatePassword}=useAuth();const navigate=useNavigate();const[p,setP]=useState('');const[c,setC]=useState('');const[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();if(p.length<12)return setError('Use at least 12 characters.');if(p!==c)return setError('The passwords do not match.');try{await updatePassword(p);navigate('/',{replace:true})}catch(x){setError(x instanceof Error?x.message:'Password update failed.')}}return <main className="min-h-screen bg-slate-950 text-slate-100 grid place-items-center p-6"><section className="w-full max-w-md rounded-3xl border border-slate-800 bg-slate-900 p-8"><h1 className="text-2xl font-bold">Create a new password</h1><form className="mt-7 space-y-5" onSubmit={submit}><input className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3" type="password" placeholder="New password" minLength={12} required value={p} onChange={e=>setP(e.target.value)}/><input className="w-full rounded-xl border border-slate-700 bg-slate-950 px-4 py-3" type="password" placeholder="Confirm password" minLength={12} required value={c} onChange={e=>setC(e.target.value)}/>{error&&<div className="text-red-300">{error}</div>}<button className="w-full rounded-xl bg-blue-600 px-4 py-3 font-semibold">Save Password</button></form></section></main>}
